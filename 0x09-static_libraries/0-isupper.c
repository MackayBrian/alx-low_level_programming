#include "main.h"
/**
 * _isupper- checks if character is upper case or lower case
 * @c: the character entered by user
 *
 * Return: 1 if upper 0 otherwise
 */
int _isupper(int c)
	{
		if (isupper(c))
		{
			return (1);
		}
		else
		{
			return (0);
		}
	}
