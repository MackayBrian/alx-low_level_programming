#include "main.h"
/**
 * *_strcpy-copies the string pointed to
 * @dest:destination
 * @src:source
 * Return:value at dest
 */
char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
	return (dest);
}
