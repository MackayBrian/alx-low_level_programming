#include <stdio.h>
/**
 * main-prints all lower case alphabets in order
 *
 * Return: return 0
 */
int main(void)
	{
		char ch;

		while (char <= 'z')
		{
			putchar(ch);
			ch++;
		}
		putchar('\n');
		return (0);
	}

