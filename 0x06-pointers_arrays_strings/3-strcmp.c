#include "main.h"
/**
 * _strcmp-compares two string
 * @s1:first string
 * @s2:second string
 * Return:flag
 */
int _strcmp(char *s1, char *s2)
{
	int i;
	int flag;

	for (i = 0; si[i] != '\0' || i = 0; s2[i] != '\0'; i++)
	{
		if (si[i] == s2[i])
		{
			flag = 1;
		}
		else
		{
			flag = 0;
		}
	}
	return (flag);
}
